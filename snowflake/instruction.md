Under Catalog, there is database.
For now we are going with bootcamp database. you can create Schema, for now we are using TEST.
Under schema you can link AWS from stage > AWS. Give bucket path folder is fine.

For free account it doesn't allow to create multiple workspace. You can select database and schema on top right
to set enviroment. If not set for queries, you need to provide
`database.schema.table`
